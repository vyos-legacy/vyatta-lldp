help: Show lldp neighbors detail

run: if [ -d $VYATTA_ACTIVE_CONFIGURATION_DIR/service/lldp ] ; then
       sudo /opt/vyatta/bin/sudo-users/vyatta-show-lldp.pl \
         --action=show-neighbor-detail
     else
       echo LLDP not configured
     fi
